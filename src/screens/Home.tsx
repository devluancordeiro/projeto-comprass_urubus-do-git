import React from 'react';
import {View} from 'react-native';
import RedButton from '../components/ui/RedButton';

const Home = () => {
  return <RedButton onPress={function (): void {}}>LOGIN</RedButton>;
};

export default Home;
